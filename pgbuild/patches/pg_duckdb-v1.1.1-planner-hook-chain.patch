diff --git a/include/pgduckdb/pgduckdb_hooks.hpp b/include/pgduckdb/pgduckdb_hooks.hpp
index 137c38f..6648810 100644
--- a/include/pgduckdb/pgduckdb_hooks.hpp
+++ b/include/pgduckdb/pgduckdb_hooks.hpp
@@ -6,6 +6,8 @@ namespace pgduckdb {
 extern int64_t executor_nest_level;
 bool IsAllowedStatement(Query *query, bool throw_error = false);
 bool IsCatalogTable(Relation rel);
+PlannedStmt *PlanPostgresQuery(Query *parse, const char *query_string, int cursor_options,
+                               ParamListInfo bound_params);
 bool ContainsPostgresTable(Node *node, void *context);
 bool NeedsDuckdbExecution(Query *query);
 bool ShouldTryToUseDuckdbExecution(Query *query);
diff --git a/src/pgduckdb_hooks.cpp b/src/pgduckdb_hooks.cpp
index e984c66..219514c 100644
--- a/src/pgduckdb_hooks.cpp
+++ b/src/pgduckdb_hooks.cpp
@@ -41,6 +41,33 @@ static ExecutorStart_hook_type prev_executor_start_hook = NULL;
 static ExecutorFinish_hook_type prev_executor_finish_hook = NULL;
 static ExplainOneQuery_hook_type prev_explain_one_query_hook = NULL;
 static emit_log_hook_type prev_emit_log_hook = NULL;
+static int postgres_planner_nest_level = 0;
+
+namespace pgduckdb {
+PlannedStmt *
+PlanPostgresQuery(Query *parse, const char *query_string, int cursor_options, ParamListInfo bound_params) {
+	PlannedStmt *stmt = nullptr;
+
+	/*
+	 * Enter the complete planner hook chain so hooks such as TimescaleDB can
+	 * initialize their per-query state. DuckdbPlannerHook_Cpp observes this
+	 * guard and forwards instead of recursively forcing DuckDB execution.
+	 */
+	postgres_planner_nest_level++;
+	PG_TRY();
+	{
+		stmt = planner_hook ? planner_hook(parse, query_string, cursor_options, bound_params)
+		                    : standard_planner(parse, query_string, cursor_options, bound_params);
+	}
+	PG_FINALLY();
+	{
+		postgres_planner_nest_level--;
+	}
+	PG_END_TRY();
+
+	return stmt;
+}
+} // namespace pgduckdb
 
 static bool
 ContainsCatalogTable(List *rtes) {
@@ -257,6 +284,10 @@ IsAllowedStatement(Query *query, bool throw_error) {
 
 static PlannedStmt *
 DuckdbPlannerHook_Cpp(Query *parse, const char *query_string, int cursor_options, ParamListInfo bound_params) {
+	if (postgres_planner_nest_level > 0) {
+		return prev_planner_hook(parse, query_string, cursor_options, bound_params);
+	}
+
 	if (pgduckdb::IsExtensionRegistered()) {
 		if (pgduckdb::NeedsDuckdbExecution(parse)) {
 			pgduckdb::TriggerActivity();
diff --git a/src/scan/postgres_table_reader.cpp b/src/scan/postgres_table_reader.cpp
index 314145b..6a2f282 100644
--- a/src/scan/postgres_table_reader.cpp
+++ b/src/scan/postgres_table_reader.cpp
@@ -1,4 +1,5 @@
 #include "pgduckdb/scan/postgres_table_reader.hpp"
+#include "pgduckdb/pgduckdb_hooks.hpp"
 #include "pgduckdb/pgduckdb_process_lock.hpp"
 #include "pgduckdb/pgduckdb_utils.hpp"
 #include "pgduckdb/pgduckdb_guc.hpp"
@@ -22,6 +23,7 @@ extern "C" {
 #include "utils/rel.h"
 #include "utils/snapmgr.h"
 #include "utils/wait_event.h"
+#include "commands/extension.h"
 }
 
 #include "pgduckdb/vendor/pg_list.hpp"
@@ -30,6 +32,28 @@ extern "C" {
 
 namespace pgduckdb {
 
+/*
+ * pgembed: TimescaleDB's loader deliberately does not load its versioned
+ * library inside a parallel worker (see timescaledb src/loader/loader.c,
+ * CalledInParallelWorker()); it relies on Postgres' own parallel-worker
+ * infrastructure to restore library state. PostgresTableReader launches
+ * workers itself via ExecInitParallelPlan()/LaunchParallelWorkers() rather
+ * than through a regular Gather node, so that expectation does not hold and
+ * the leader can block forever in ParallelFinish waiting on a worker that
+ * never completes.
+ *
+ * Until that is fixed upstream, fall back to a single-process scan whenever
+ * TimescaleDB is installed. This costs parallelism only for the pg_duckdb +
+ * TimescaleDB combination and leaves pg_duckdb-only deployments untouched.
+ */
+static bool
+TimescaleDBIsInstalled() {
+	if (!IsTransactionState()) {
+		return false;
+	}
+	return get_extension_oid("timescaledb", true) != InvalidOid;
+}
+
 PostgresTableReader::PostgresTableReader()
     : table_scan_query_desc(nullptr), table_scan_planstate(nullptr), parallel_executor_info(nullptr),
       parallel_worker_readers(nullptr), slot(nullptr), nworkers_launched(0), nreaders(0), next_parallel_reader(0),
@@ -63,7 +87,7 @@ PostgresTableReader::InitUnsafe(const char *table_scan_query, bool count_tuples_
 
 	char persistence = get_rel_persistence(rte->relid);
 
-	PlannedStmt *planned_stmt = standard_planner(query, table_scan_query, 0, nullptr);
+	PlannedStmt *planned_stmt = PlanPostgresQuery(query, table_scan_query, 0, nullptr);
 
 	table_scan_query_desc = CreateQueryDesc(planned_stmt, table_scan_query, GetActiveSnapshot(), InvalidSnapshot,
 	                                        None_Receiver, nullptr, nullptr, 0);
@@ -74,6 +98,7 @@ PostgresTableReader::InitUnsafe(const char *table_scan_query, bool count_tuples_
 
 	bool run_scan_with_parallel_workers = persistence != RELPERSISTENCE_TEMP;
 	run_scan_with_parallel_workers &= CanTableScanRunInParallel(table_scan_query_desc->planstate->plan);
+	run_scan_with_parallel_workers &= !TimescaleDBIsInstalled();
 
 	/* Temp tables cannot be excuted with parallel workers, and whole plan should be parallel aware */
 	if (run_scan_with_parallel_workers) {
