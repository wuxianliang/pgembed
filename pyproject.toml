[project]
name = "pgembed" # Required
version = "0.3.0rc1"  # PostgreSQL 18 pre-release
description = "Self-contained postgres server for your python applications" # Required
readme = "README.md" # Optional
requires-python = ">=3.12"
license = {file = "LICENSE"}
urls={repository="https://github.com/Ladybug-Memory/pgembed"}
authors=[{ name="Oscar Moll", email="orm@csail.mit.edu" }, { name="Arun Sharma", email="arun@sharma-home.net" }]
keywords=["postgresql", "pgvector", "pgembed", "vectorchord", "timescaledb", "rag"]
dependencies = [
    "fasteners>=0.19",
    "platformdirs>=4.0.0",
    "psutil>=5.9.0",
]

[project.optional-dependencies]
dev = [
    "sysv_ipc",
]
test = [
    "pytest",
    "psycopg2-binary",
    "sqlalchemy>=2",
    "sqlalchemy-utils"
]

[tool.setuptools.packages.find]
where = ["src"]  # list of folders that contain the packages (["."] by default)
include = ["pgembed*", "pgembed_pg*"]  # package names should match these glob patterns

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: requires a complete bundled PostgreSQL 18 installation",
    "tigerfs_mount: requires a working FUSE/TigerFS mount environment",
]

[tool.cibuildwheel]
test-extras = "test"
test-command = "bash -x {project}/cibuildwheel_test.bash {project}"

[build-system]
# These are the assumed default build requirements from pip:
# https://pip.pypa.io/en/stable/reference/pip/#pep-517-and-518-support
requires = ["setuptools>=58.0.0", "wheel"]
build-backend = "setuptools.build_meta"
